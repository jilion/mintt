- title "Documents > #{@document.title}"

.text.sheet
  .right_box
    %h3= @document.title
  .application_date
    %p.edit= link_to "edit document", edit_admin_document_path(@document)
    
  .spacer
  %p= "Module ID: #{@document.module_id}"
  
  #document{ :class => pretty_class(@document) }
    %p= link_to pretty_file(@document, :width => 400), @document.url