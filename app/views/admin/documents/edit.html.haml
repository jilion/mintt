= title "Edit document: #{content_tag(:em, truncate_middle(@document.title, :length => 30))}"

= page_title "Editing document: #{content_tag(:em, truncate_middle(@document.title, :length => 30))}"

= render :partial => 'form'