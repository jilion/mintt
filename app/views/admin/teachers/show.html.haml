- title " | Teachers > #{@teacher.name || @teacher.email}"

.text.sheet
  .right_box
    %h3= @teacher.name || @teacher.email
    %p.email
      = image_tag "/images/admin/email.png", :alt => "Email"
      = mail_to @teacher.email
  .application_date
    %p.edit= link_to "edit teacher", edit_admin_teacher_path(@teacher)
    
  .spacer
  .card