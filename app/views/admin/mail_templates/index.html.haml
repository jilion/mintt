- title "Mail templates"

.text
  %table
    - 2.times do
      %col{ :class => "even" }
    %tr
      %th Title
      %th Actions
      
    - @mail_templates.each do |mail_template|
      = render 'mail_template', :object => mail_template