%table
  %colgroup
    - 8.times do
      %col{ :class => cycle("even", "odd") }
  %tr
    %th= link_to "’10", admin_users_path(sort_parameters('selected_at')), :remote => true
    %th= link_to "Gender", admin_users_path(sort_parameters('gender')), :remote => true
    %th= link_to "Name", admin_users_path(sort_parameters('last_name')), :remote => true
    %th= link_to "School", admin_users_path(sort_parameters('school')), :remote => true
    %th= link_to "Supervisor", admin_users_path(sort_parameters('thesis_supervisor')), :remote => true
    %th= link_to "Application Date", admin_users_path(sort_parameters('confirmed_at')), :remote => true
    %th
    %th
  = render @users
  
- if params.key? :all
  .right_links= link_to "View with paginate", admin_users_path, :remote => true
- elsif @users.total_entries > User.per_page
  .right_links= link_to "View all", admin_users_path(:all => true), :remote => true
  = will_paginate(:params => { :format => nil }, :renderer => 'WillPaginate::ViewHelpers::LinkRendererRemote')
